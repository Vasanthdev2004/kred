// Reuse the OpenGraph image for the Twitter/X card so shared links render the
// full 1200x630 summary_large_image instead of the small default card.
export { default, alt, size, contentType } from "./opengraph-image";
